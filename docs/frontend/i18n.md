---
sidebar_position: 3
title: "Internationalization (i18n)"
description: "Standardized i18n setup for all FitNexa web applications"
---
# Internationalization (i18n)

FitNexa supports global expansion through a robust i18n strategy using `react-i18next`.

## 🌐 Supported Locales
- **en**: English (Default)
- **it**: Italian
- **es**: Spanish
- **fr**: French

## 🏗️ Structure
Each app contains a `locales/` directory with JSON files for each language:
```text
src/i18n/
├── locales/
│   ├── en.json
│   ├── it.json
│   └── ...
└── config.ts
```

## ⌨️ Naming Conventions
Use dot-separated, lowercase keys based on the section of the app:
- `auth.login.title`
- `dashboard.stats.active_members`
- `common.buttons.save`

## 🚀 Usage in Components

```tsx
import { useTranslation } from 'react-i18next';

function Welcome() {
  const { t } = useTranslation();
  return <h1>{t('dashboard.welcome')}</h1>;
}
```

## 🔄 Adding a Language
1. Create the new JSON file in `src/i18n/locales/`.
2. Register the locale in the `i18next` initialization in `config.ts`.
3. Add the language to the `@fitnexa/shared` locale types if applicable.

---
Related: [Mobile i18n](../mobile/i18n.md) · [Frontend Overview](overview.md)
